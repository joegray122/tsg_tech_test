## SEiT Technical Test Solution

Prerequisites: mock API server is running on `http://localhost:3000` and in it's initial state

To run tests using cypress UI: 

`npx cypress open` 

or headlessly with: 

`npx cypress run`